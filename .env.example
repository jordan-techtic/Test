# Production / preview only — dev uses the Vite `/api` proxy (see vite.config.ts).
VITE_API_URL=http://174.138.72.184:8989
# Optional alias (trailing /api is stripped automatically):
# VITE_API_BASE_URL=http://174.138.72.184:8989/api
